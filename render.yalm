services:
  - type: web
    name: apicoin
    env: python
    region: singapore
    plan: free
    buildCommand: |
         pip install -r requirements.txt
         npx playwright install --with-deps

    startCommand: python apicoin.py
    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /tmp/playwright

